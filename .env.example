# Copy this file to .env.local and add your real key.
# Get a key from Google AI Studio: https://aistudio.google.com/app/apikey
# The key lives ONLY here / in the serverless env — never in client code.
GEMINI_API_KEY=placeholder
